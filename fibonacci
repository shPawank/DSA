#include<stdio.h>
#include<stdlib.h>
#include<conio.h>
int fib(int);
void main(){
int n,res;
printf("enter the value of n");
scanf("%d",&n);
res=fib(n);

printf("%d",res);
getch();

}
int fib(int n){
if(n==0||n==1)
return n;
else
return (fib(n-1)+fib(n-2));




}
